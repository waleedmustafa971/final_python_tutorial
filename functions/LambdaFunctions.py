# Lambda functions are small, anonymous functions that can have any number of arguments but only one expression. They are useful for short operations.

#syntax:

# lambda arguments: expression

#example:

add = lambda x, y: x + y
print(add(2, 3))  # Output: 5
